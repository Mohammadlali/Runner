# Install Unreal Engine on a PUBLIC repository's runner, and time it.
#
# WHAT HAS BEEN MEASURED SO FAR, both on this workflow:
#   run 33016837041, no arguments   Setup.sh KILLED at the 6 GB disk floor
#                                   after 637 s, tree 116163 MB (113 GB)
#   run 33018377868, --no-cache     Setup.sh rc=0 in 295 s, tree 96502 MB
#                                   (94 GB), clone 49 s, whole job 445 s
# So --no-cache is the difference between finishing and not, and it is the
# default here. Setup.sh's own "31113.5 MiB" is the DOWNLOAD size; the tree on
# disk is three times that. A number off a tool's banner is not the number on
# the disk.
#
# THIS REPO IS NEVER CHECKED OUT. Only the private one is, into `work/`. Any
# helper a step calls must live in the PRIVATE repo: run 33018377868 measured
# everything correctly and then lost its report to
# `bash: /home/runner/work/Runner/Runner/push-with-merge.sh: No such file or
# directory`, rc 127 -- and reported the step, and the whole job, as SUCCESS.
#
# NOTHING IS BUILT HERE. Clone, Setup.sh, report. Compiling on 4 cores inside
# a 6-hour job is a separate question.
#
# Every `run:` block is `bash -e`, which kills a step before a later line can
# record anything, so every stage is `set +e` with an explicit rc.
#
# The token never reaches the log: it is used inside a clone URL, and GitHub
# masks secret values. Do not echo the URL.
name: engine

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'engine branch or tag, e.g. 5.8'
        default: '5.8'
      setup_args:
        description: 'Setup.sh args. --no-cache stops GitDependencies keeping
          every downloaded pack alongside what it extracted; without it the
          tree passed 113 GB and was killed. Add --exclude=Android
          --exclude=IOS --exclude=VisionOS to drop platforms this project
          never targets (folder names measured, not guessed -- see
          Reports/cloud_host/engine_deps_help.txt).'
        default: '--no-cache'
      commit:
        description: 'push the report to the private repo'
        type: boolean
        default: true

permissions: {}

jobs:
  engine:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - name: Is the secret present at all
        id: secret
        env:
          TOK: ${{ secrets.TBS_PRIVATE_TOKEN }}
        run: |
          set +e
          if [ -z "$TOK" ]; then
            echo "ok=no" >> "$GITHUB_OUTPUT"
            echo "TBS_PRIVATE_TOKEN is NOT set on this repository."
          else
            echo "ok=yes" >> "$GITHUB_OUTPUT"
            echo "secret present"
          fi

      - uses: actions/checkout@v4
        if: steps.secret.outputs.ok == 'yes'
        with:
          repository: Mohammadlali/Claud-Cloud-Project
          ref: ${{ vars.TBS_BRANCH || 'claude/gateway-auth-fix-nr7k0b' }}
          token: ${{ secrets.TBS_PRIVATE_TOKEN }}
          fetch-depth: 0
          path: work

      - name: Make room, and say how much there was
        if: steps.secret.outputs.ok == 'yes'
        run: |
          set +e
          echo "before : $(df -BG --output=avail / | tail -1 | tr -d ' ')"
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc \
                      /usr/local/share/boost /usr/local/.ghcup \
                      /usr/share/swift /opt/hostedtoolcache 2>/dev/null
          echo "after  : $(df -BG --output=avail / | tail -1 | tr -d ' ')"
          sudo mkdir -p /mnt/ue
          sudo chown "$(id -u):$(id -g)" /mnt/ue
          echo "cores  : $(nproc)"
          echo "ram    : $(free -g | awk '/^Mem:/{print $2}') GB"

      - name: Clone the engine
        id: clone
        if: steps.secret.outputs.ok == 'yes'
        env:
          TOK: ${{ secrets.TBS_PRIVATE_TOKEN }}
          REF: ${{ inputs.ref }}
        run: |
          set +e
          BEGAN=$(date +%s)
          git clone --depth 1 --branch "$REF" \
            "https://x-access-token:${TOK}@github.com/EpicGames/UnrealEngine.git" \
            /mnt/ue > /tmp/clone.log 2>&1
          RC=$?
          SECS=$(( $(date +%s) - BEGAN ))
          echo "rc=$RC"     >> "$GITHUB_OUTPUT"
          echo "secs=$SECS" >> "$GITHUB_OUTPUT"
          SZ=$(du -sm /mnt/ue 2>/dev/null | cut -f1)
          echo "size=${SZ:-0}" >> "$GITHUB_OUTPUT"
          echo "clone rc=$RC in ${SECS}s, tree ${SZ}MB"
          if [ "$RC" -ne 0 ]; then
            echo "--- last 20 lines of the clone log ---"
            tail -20 /tmp/clone.log
          fi

      - name: Setup.sh -- the binary dependencies
        id: setup
        if: ${{ steps.secret.outputs.ok == 'yes' && steps.clone.outputs.rc == '0' }}
        env:
          ARGS: ${{ inputs.setup_args }}
        run: |
          set +e
          PROG=/tmp/progress.txt
          : > "$PROG"
          BEGAN=$(date +%s)
          ( cd /mnt/ue && ./Setup.sh $ARGS ) > /tmp/setup.log 2>&1 &
          PID=$!

          # The trail is echoed to the LOG as well as the report, because two
          # runs in a row lost their report to a broken push and the log was
          # the only place the numbers survived.
          KILLED=no
          while kill -0 "$PID" 2>/dev/null; do
            FREE=$(df -BG --output=avail / | tail -1 | tr -dc '0-9')
            SZ=$(du -sm /mnt/ue 2>/dev/null | cut -f1)
            EL=$(( $(date +%s) - BEGAN ))
            echo "t=${EL}s free=${FREE}G tree=${SZ}MB" >> "$PROG"
            if [ "${FREE:-99}" -lt 6 ]; then
              echo "t=${EL}s FLOOR HIT at ${FREE}G -- killing Setup.sh" >> "$PROG"
              kill -9 "$PID" 2>/dev/null
              KILLED=yes
              break
            fi
            sleep 30
          done
          wait "$PID" 2>/dev/null
          RC=$?
          SECS=$(( $(date +%s) - BEGAN ))
          SZ=$(du -sm /mnt/ue 2>/dev/null | cut -f1)
          echo "rc=$RC"         >> "$GITHUB_OUTPUT"
          echo "secs=$SECS"     >> "$GITHUB_OUTPUT"
          echo "size=${SZ:-0}"  >> "$GITHUB_OUTPUT"
          echo "killed=$KILLED" >> "$GITHUB_OUTPUT"
          echo "setup rc=$RC in ${SECS}s, tree ${SZ}MB, killed=$KILLED"
          echo "--- the progress trail ---"
          cat "$PROG"

      - name: Write the report and push it to the PRIVATE repo
        id: deliver
        if: ${{ always() && steps.secret.outputs.ok == 'yes' }}
        working-directory: work
        env:
          REF:    ${{ inputs.ref }}
          ARGS:   ${{ inputs.setup_args }}
          C_RC:   ${{ steps.clone.outputs.rc }}
          C_SECS: ${{ steps.clone.outputs.secs }}
          C_SIZE: ${{ steps.clone.outputs.size }}
          S_RC:   ${{ steps.setup.outputs.rc }}
          S_SECS: ${{ steps.setup.outputs.secs }}
          S_SIZE: ${{ steps.setup.outputs.size }}
          S_KILL: ${{ steps.setup.outputs.killed }}
          RUN_ID: ${{ github.run_id }}
          DO_COMMIT: ${{ inputs.commit }}
          BRANCH: ${{ vars.TBS_BRANCH || 'claude/gateway-auth-fix-nr7k0b' }}
        run: |
          set +e
          OUT="Reports/cloud_host/engine_public_runner_${RUN_ID}.txt"
          mkdir -p Reports/cloud_host
          {
            echo "Installing Unreal Engine on a PUBLIC repository's hosted runner."
            echo ""
            echo "instrument : Mohammadlali/Runner .github/workflows/engine.yml"
            echo "run        : ${RUN_ID}"
            echo "when       : $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "engine ref : ${REF}"
            echo "setup args : ${ARGS:-<none>}"
            echo "machine    : $(nproc) cores, $(free -g | awk '/^Mem:/{print $2}') GB ram"
            echo ""
            echo "== clone (git clone --depth 1) =="
            echo "rc         : ${C_RC:-<step did not run>}"
            echo "seconds    : ${C_SECS:-<none>}"
            echo "tree MB    : ${C_SIZE:-<none>}"
            echo ""
            echo "== Setup.sh (binary dependencies) =="
            echo "rc         : ${S_RC:-<step did not run>}"
            echo "seconds    : ${S_SECS:-<none>}"
            echo "tree MB    : ${S_SIZE:-<none>}"
            echo "killed     : ${S_KILL:-<none>}   (disk floor 6G)"
            echo ""
            echo "disk free now : $(df -BG --output=avail / | tail -1 | tr -d ' ')"
            echo ""
            echo "== VERDICT =="
            if [ "${S_KILL}" = "yes" ]; then
              echo "DOES NOT FIT at these arguments. Setup.sh was still running"
              echo "when free space reached the floor."
            elif [ "${C_RC}" != "0" ]; then
              echo "NOT MEASURED past the clone. The clone itself failed, so"
              echo "nothing here says whether the engine fits."
            elif [ "${S_RC}" = "0" ]; then
              echo "INSTALLED. This is the real installed size and the real"
              echo "wall-clock cost. It says NOTHING about compiling it, and"
              echo "nothing about persistence: this disk dies with the job."
            else
              echo "Setup.sh FAILED for a reason that is not disk. Its own"
              echo "last lines follow."
            fi
            echo ""
            echo "== progress trail, every 30s =="
            cat /tmp/progress.txt 2>/dev/null || echo "(none)"
            echo ""
            echo "== last 40 lines of Setup.sh =="
            tail -40 /tmp/setup.log 2>/dev/null || echo "(none)"
          } > "$OUT"
          echo "wrote $OUT"

          if [ "$DO_COMMIT" != "true" ]; then
            echo "commit not requested -- stopping."
            echo "delivered=skipped" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ ! -f Tools/push_with_merge.sh ]; then
            echo "Tools/push_with_merge.sh is missing from the checkout."
            echo "The helper is ABSENT -- that is not the same answer as a"
            echo "push that was refused."
            echo "delivered=no" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          git config user.name  "tbs-runner"
          git config user.email "noreply@github.com"
          git add "$OUT"
          git commit -m "engine: install measured on the public runner, run ${RUN_ID}"
          if [ $? -ne 0 ]; then
            echo "nothing to commit."
            echo "delivered=no" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          H=$(git rev-parse --short HEAD)
          python3 Tools/record_commit.py "$H" \
            "Unreal Engine install measured on the public runner (\`${OUT}\`), run ${RUN_ID}: clone rc=${C_RC} in ${C_SECS}s, Setup.sh rc=${S_RC} in ${S_SECS}s with \`${ARGS}\`, tree ${S_SIZE}MB, killed=${S_KILL}. Written by \`Runner\` \`engine.yml\`, not by a person."
          if [ $? -ne 0 ]; then
            echo "record_commit failed -- not pushing an unrecorded commit."
            echo "delivered=no" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          git add Team/CHANGELOG.md
          git commit -m "changelog: record ${H}"

          bash Tools/push_with_merge.sh "${BRANCH}"
          if [ $? -ne 0 ]; then
            echo "delivered=no" >> "$GITHUB_OUTPUT"
            echo "THE REPORT DID NOT REACH THE REPOSITORY."
            exit 1
          fi
          echo "delivered=yes" >> "$GITHUB_OUTPUT"

      - name: Verdict
        if: ${{ always() }}
        run: |
          set +e
          if [ "${{ steps.secret.outputs.ok }}" != "yes" ]; then
            echo "NOT MEASURED. Nothing ran, and that is not a success."
            exit 1
          fi
          if [ "${{ steps.clone.outputs.rc }}" != "0" ]; then
            echo "clone rc=${{ steps.clone.outputs.rc }} -- see the report."
            exit 1
          fi
          if [ "${{ steps.setup.outputs.rc }}" != "0" ]; then
            echo "Setup.sh rc=${{ steps.setup.outputs.rc }}, killed=${{ steps.setup.outputs.killed }}, tree ${{ steps.setup.outputs.size }} MB -- see the report."
            exit 1
          fi
          # A measurement that never reached the repository is a measurement
          # nobody can cite. Two runs were lost this way before this check
          # existed, and both jobs reported success.
          if [ "${{ steps.deliver.outputs.delivered }}" = "no" ]; then
            echo "engine installed: ${{ steps.setup.outputs.size }} MB in ${{ steps.setup.outputs.secs }}s"
            echo "BUT THE REPORT DID NOT REACH THE REPOSITORY, so it does not"
            echo "count. The numbers are in this job's log only."
            exit 1
          fi
          echo "engine installed: ${{ steps.setup.outputs.size }} MB in ${{ steps.setup.outputs.secs }}s, report delivered"
